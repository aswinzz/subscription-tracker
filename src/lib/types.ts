export interface ITEM {
    id: number,
    serviceName: string,
    amount: number,
    currency: string,
    cycle: string,
    billingDate: string,
    notes?: string,
    website: string,
    logo?: string,
}